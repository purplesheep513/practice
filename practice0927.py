import sys

target = int(sys.stdin.readline().rstrip())
numbers = list(map(int,sys.stdin.readline().rstrip().split()))
visit = [[0,0] for _ in range(len(numbers))]
dx = [-1,1]

def solution():
    answer = 0
    return answer
